= form_for [:admin, @admin_section] do |f|
  - if @admin_section.errors.any?
    #error_explanation
      %h2= "#{pluralize(@admin_section.errors.count, "error")} prohibited this admin_page from being saved:"
      %ul
        - @admin_section.errors.full_messages.each do |message|
          %li= message
  - frame_id = dom_id @admin_section, "edit_turbo_frame"
  = turbo_frame_tag frame_id do
    = f.hidden_field :_destroy
    = f.hidden_field :id
    = f.hidden_field :page_id
    = f.text_field :name 
    = link_to "×", '#', class: "remove_fields"
